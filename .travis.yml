language: node_js
node_js:
  - '8'
branches:
  only:
    - master
    - dev
cache:
  directories:
    - node_modules
env:
  global:
before_script:
  - npm i -g yarn
  - yarn
script:
  - npm run build
before_deploy:
  - git config --local user.name "wxrbwran"
  - git config --local user.email "wxrbw@qq.com"
after_success:
  - ssh ubuntu@123.206.90.112 -p qingfei77520 'cd /home/ubuntu/sites/coinfans && git pull && npm run build'